---
kind: pipeline
type: docker
name: Test

platform:
  os: linux
  arch: amd64

steps:
  - name: write
    image: ubuntu
    commands: 
    - echo "Hello world!"

trigger:
    branch: 
    - master

---
kind: pipeline
type: docker
name: qa

depends_on:
  - Test

platform:
  os: linux
  arch: amd64

steps:
  - name: write
    image: ubuntu
    commands: 
    - echo ${name}

trigger:
    branch: 
    - master
    event:
    - promote
    target:
    - qa
    environment:
      name: alan 

    