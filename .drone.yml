---
kind: pipeline
type: docker
name: Test

platform:
  os: linux
  arch: amd64

steps:
  - name: write
    image: ubuntu
    commands: 
    - echo "Hello world!"

trigger:
    branch: 
    - master

---
kind: pipeline
type: docker
name: QA

platform:
  os: linux
  arch: amd64

steps:
  - name: write
    image: ubuntu
    commands: 
    - echo "Promoted!"

trigger:
    branch: 
    - master
    event:
    - promote
    target:
    - QA

    